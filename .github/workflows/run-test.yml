name: Run Tests

on:
  pull_request:
  push:
    branches:
      - master

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    name: P${{ matrix.php }} - L${{ matrix.laravel }} - ${{ matrix.dependency-version }}
    runs-on: ubuntu-latest
    timeout-minutes: 10

    strategy:
      fail-fast: false
      matrix:
        php: ['8.2', '8.3', '8.4', '8.5']
        laravel: ['11.*', '12.*', '13.*']
        dependency-version: [prefer-stable]
        include:
          - laravel: '11.*'
            testbench: '9.*'
          - laravel: '12.*'
            testbench: '10.*'
          - laravel: '13.*'
            testbench: '11.*'
        exclude:
          # Laravel 11 predates PHP 8.5 and was never supported on it.
          - php: '8.5'
            laravel: '11.*'
          # Laravel 13 requires php ^8.3, so there is no 8.2 leg to test.
          - php: '8.2'
            laravel: '13.*'

    steps:
      # Checkout Code (current branch)
      - name: Checkout
        uses: actions/checkout@v7.0.1

      # Install PHP
      - name: Setup PHP, with composer and extensions
        uses: shivammathur/setup-php@v2 #https://github.com/shivammathur/setup-php
        with:
          php-version: ${{ matrix.php }}
          extensions: gmp, mbstring, openssl, sqlite, pdo_sqlite
          coverage: none

      # ::add-matcher:: is a GitHub Actions workflow command: it hands the runner
      # a file of regexes, and any line printed by a LATER step that matches one
      # becomes an inline PR annotation (file + line) instead of plain log text.
      # The two JSON files are written into the tool cache by setup-php, not by
      # GitHub. Convenience only — it cannot affect pass/fail, and if a path is
      # wrong the runner registers nothing and says nothing.
      - name: Setup problem matchers
        run: |
          echo "::add-matcher::${{ runner.tool_cache }}/php.json"
          echo "::add-matcher::${{ runner.tool_cache }}/phpunit.json"

      # Resolve a fresh dependency tree for this matrix leg. The committed
      # composer.lock is deliberately bypassed so each leg tests real
      # resolution rather than a single pinned tree.
      - name: Install Composer dependencies
        run: |
          composer require --dev --no-update --no-interaction "laravel/framework:${{ matrix.laravel }}" "orchestra/testbench:${{ matrix.testbench }}"
          composer update --${{ matrix.dependency-version }} --prefer-dist --no-interaction --no-progress

      # Run tests. --no-coverage is required because phpunit.xml declares a
      # clover report: with `coverage: none` above there is no driver to
      # satisfy it, and PHPUnit exits non-zero on that runner warning.
      # Coverage stays owned by ci.yml, which runs with xdebug.
      - name: Execute tests
        run: vendor/bin/phpunit --no-coverage
